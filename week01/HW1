select current_timestamp, current_database();
select version();
select 'Hello, SQL!';
create database analysis;
create table teachers (id bigserial, hire_date date);
why semicolons are imprtant is becoues that is to show were the command ends. 
